class Solution:
    def checkEqual(self, a, b) -> bool:
        #code here
        if len(a) != len(b):
            return False
        
        a.sort()
        b.sort()
        
        i=0
        
        for n in a:
           if n != b[i]:
               return False
            
           i += 1
        
        return True   