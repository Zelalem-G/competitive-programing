from collections import Counter

class Solution:
    def isSubset(self, a, b):
        
        countA = Counter(a)
        countB = Counter(b)
        
        for e, c in countB.items():
            if countA[e] < c:
                return False
        return True